import { gameMetadata } from '@/lib/games'

export const metadata = gameMetadata('aventura-espacio')

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
